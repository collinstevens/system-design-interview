{
  "name": "fixed-window-counter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "mise exec -- tsc --noEmit",
    "build": "mise exec -- tsc && mise exec -- node -e \"require('node:fs').copyFileSync('src/fixed-window-counter.lua', 'dist/fixed-window-counter.lua')\"",
    "demo": "bun run build && mise exec -- node dist/demo.js",
    "redis:up": "docker compose up -d --wait",
    "redis:down": "docker compose down"
  },
  "dependencies": {
    "effect": "catalog:",
    "redis": "catalog:"
  }
}
